<header class="main-header">
  <div class="container">
    <h1 class="page-title">Servicios</h1>

    <ol class="breadcrumb pull-right">
      <li><%= link_to "Home", root_path %></li>
      <li><%= link_to "Negocios & Servicios", pages_trading_apps_path %></li>
      <li class="active">Servicios</li>
    </ol>
  </div>
</header>

<div class="container">
  <section class="margin-bottom">
    <div class="row">
      <div class="col-md-12">
        <h2 class="right-line">Estrategias<h2>
      </div>
      <div class="col-md-12">
        <div class="footer-widget">
          <% @strategies.each do |strategy| %>
            <div class="media col-md-4 media-first-resource">
              <%= link_to "<img class='media-object' src='#{strategy.logo.url(:mini_thumb)}' width='75' height='75' alt='image'>".html_safe, product_path(strategy), class: "pull-left" %>
              <div class="media-body">
                <h4 class="media-heading"><%= link_to strategy.title, product_path(strategy) %></h4>
                <p class="product-thumbnail-text"><%= strategy.resume %></p>
              </div>
            </div>
          <% end %>
        </div>
      </div>
    </div> <!-- row -->
    <div class="row">
      <div class="col-md-12 text-right">
        <%= link_to "See All", pages_strategies_path, class: "btn btn-ar btn-primary btn-lg" %>
      </div>
    </div>
  </section>
</div> <!-- container -->


<div class="container">
  <section class="margin-bottom">
    <div class="row">
      <div class="col-md-12">
        <h2 class="right-line">Trading Apps<h2>
      </div>
      <div class="col-md-12">
        <div class="footer-widget">
          <% @indicators.each do |indicator| %>
            <div class="media col-md-4">
              <%= link_to "<img class='media-object' src='#{indicator.logo.url(:mini_thumb)}' width='75' height='75' alt='image'>".html_safe, product_path(indicator), class: "pull-left" %>
              <div class="media-body">
                <h4 class="media-heading"><%= link_to indicator.title, product_path(indicator) %></h4>
                <p class="product-thumbnail-text"><%= indicator.resume %></p>
              </div>
            </div>
          <% end %>
        </div>
      </div>
    </div> <!-- row -->
    <div class="row">
      <div class="col-md-12 text-right">
        <%= link_to "See All", pages_indicators_path, class: "btn btn-ar btn-primary btn-lg" %>
      </div>
    </div>
  </section>
</div> <!-- container -->


<div class="container">
  <section class="margin-bottom">
    <div class="row">
      <div class="col-md-12">
        <h2 class="right-line">Portafolios<h2>
      </div>
      <div class="col-md-12">
        <div class="footer-widget">
          <% @portfolios.each do |portfolio| %>
            <div class="media col-md-4">
              <%= link_to "<img class='media-object' src='#{portfolio.logo.url(:mini_thumb)}' width='75' height='75' alt='image'>".html_safe, product_path(portfolio), class: "pull-left" %>
              <div class="media-body">
                <h4 class="media-heading"><%= link_to portfolio.title, product_path(portfolio) %></h4>
                <p class="product-thumbnail-text"><%= portfolio.resume %></p>
              </div>
            </div>
          <% end %>
        </div>
      </div>
    </div> <!-- row -->
    <div class="row">
      <div class="col-md-12 text-right">
        <%= link_to "See All", pages_portfolios_path, class: "btn btn-ar btn-primary btn-lg" %>
      </div>
    </div>
  </section>
</div> <!-- container -->

<div class="container">
  <section class="margin-bottom">
    <div class="row">
      <div class="col-md-12">
        <h2 class="right-line">Cursos & Otros<h2>
      </div>
      <div class="col-md-12">
        <div class="footer-widget">
          <% @education.each do |education| %>
            <div class="media col-md-4">
              <%= link_to "<img class='media-object' src='#{education.logo.url(:mini_thumb)}' width='75' height='75' alt='image'>".html_safe, product_path(education), class: "pull-left" %>
              <div class="media-body">
                <h4 class="media-heading"><%= link_to education.title, product_path(education) %></h4>
                <p class="product-thumbnail-text"><%= education.resume %></p>
              </div>
            </div>
          <% end %>
        </div>
      </div>
    </div> <!-- row -->
    <div class="row">
      <div class="col-md-12 text-right">
        <%= link_to "See All", pages_education_path, class: "btn btn-ar btn-primary btn-lg" %>
      </div>
    </div>
  </section>
</div> <!-- container -->
